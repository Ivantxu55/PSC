package domain.jdo;

/**
 * Enumeración de colores para el coche.
 * @see Coche
 */
public enum Color {

    /** Color negro. */
    Black,

    /** Color azul. */
    Blue,

    /** Color verde. */
    Green,

    /** Color rojo. */
    Red,

    /** Color plata. */
    Silver,

    /** Color blanco. */
    White
}